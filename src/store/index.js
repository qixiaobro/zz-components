import Vue from 'vue';
import Vuex from 'vuex';
Vue.use(Vuex);

var getters = {
    state: function (state) {
        return state;
    }
}

export default new Vuex.Store({
    state:{

    },
    mutations: {
  
    }
  });